import Root from './button.svelte';
import { type ButtonProps, type ButtonSize, type ButtonVariant, buttonVariants } from './types.ts';

export {
    type ButtonProps,
    type ButtonProps as Props,
    type ButtonSize,
    type ButtonVariant,
    buttonVariants,
    Root,
    //
    Root as Button,
};
